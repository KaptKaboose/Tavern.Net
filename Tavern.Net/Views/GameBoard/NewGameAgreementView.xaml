<UserControl x:Class="Tavern.Net.Views.GameBoard.NewGameAgreementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Tavern.Net.ViewModels"
             xmlns:views="clr-namespace:Tavern.Net.Views"
             xmlns:behaviors="clr-namespace:Tavern.Net.Behaviors"
             xmlns:conv="clr-namespace:Tavern.Net.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:GameBoardViewModel}"
             d:DesignWidth="1600" d:DesignHeight="950">
    <!-- New Game agreement (online): pressing 'N' asks the other player first — the asker has
         already agreed by pressing it, the other player sees Agree/Decline. Once both agree, both
         move on to the sideboard panel. Placed after the panel and last of the modals so an
         incoming request always surfaces on top of whatever else is open. No scrim-click to
         dismiss — Cancel/Decline tells the other side. -->
    <Grid
          Visibility="{Binding IsNewGameAgreementOpen, Converter={StaticResource BoolToVisibility}}"
          d:IsHidden="True">
        <Border Background="#CC000000"/>

        <Border MinWidth="340" MaxWidth="420" Padding="24" HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="#2D3748" BorderBrush="White" BorderThickness="2" CornerRadius="10">
            <StackPanel>
                <TextBlock Text="Start a new game?" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,8"/>
                <TextBlock Text="{Binding AgreementMessage}" Foreground="#A0AEC0" TextWrapping="Wrap" Margin="0,0,0,16"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Style="{StaticResource ActionButtonStyle}" Content="Agree" Margin="0,0,8,0"
                            Command="{Binding AgreeToNewGameCommand}"
                            Visibility="{Binding ShowAgreeButton, Converter={StaticResource BoolToVisibility}}"/>
                    <Button Style="{StaticResource ActionButtonStyle}" Content="{Binding AgreementCancelLabel}"
                            Command="{Binding CancelNewGameAgreementCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
