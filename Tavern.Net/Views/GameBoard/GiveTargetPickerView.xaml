<UserControl x:Class="Tavern.Net.Views.GameBoard.GiveTargetPickerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Tavern.Net.ViewModels"
             xmlns:views="clr-namespace:Tavern.Net.Views"
             xmlns:behaviors="clr-namespace:Tavern.Net.Behaviors"
             xmlns:conv="clr-namespace:Tavern.Net.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:GameBoardViewModel}"
             d:DesignWidth="1600" d:DesignHeight="950">
    <!-- Give's target picker: shared by both entry points (a specific card via the Zoom overlay's
         own Give button, or a blind top-N via Main Deck's 'P' chord) — see GameBoardViewModel.
         ConfirmGive. Nothing has actually moved yet; Cancel (or the scrim) just closes this. -->
    <Grid
          Visibility="{Binding IsGiveTargetPickerOpen, Converter={StaticResource BoolToVisibility}}"
          d:IsHidden="True">
        <Button Command="{Binding CancelGiveCommand}" Focusable="False" Cursor="Arrow">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="#CC000000"/>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Border MaxWidth="360" Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="#2D3748" BorderBrush="White" BorderThickness="2" CornerRadius="10">
            <StackPanel>
                <TextBlock Text="Give to opponent's..." FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,16"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Style="{StaticResource ActionButtonStyle}" Content="Field" Margin="0,0,12,0" Command="{Binding ConfirmGiveToFieldCommand}"/>
                    <Button Style="{StaticResource ActionButtonStyle}" Content="Sealed" Command="{Binding ConfirmGiveToSealedCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
