<UserControl x:Class="Tavern.Net.Views.GameBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Tavern.Net.ViewModels"
             xmlns:views="clr-namespace:Tavern.Net.Views"
             xmlns:behaviors="clr-namespace:Tavern.Net.Behaviors"
             xmlns:conv="clr-namespace:Tavern.Net.Converters"
             xmlns:gb="clr-namespace:Tavern.Net.Views.GameBoard"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:GameBoardViewModel}"
             d:DesignWidth="1600" d:DesignHeight="950">
    <!-- Styles, templates and converters for the board and its panels live in Views/GameBoard/GameBoard*.xaml
         (merged in App.xaml); each overlay is its own control in that folder. -->
    <!-- Shared size scope: the copy of the phase bar layered over the Opponent panel borrows the
         meta-action bar's row height and left-column width (SharedSizeGroup below), so it lands
         exactly on top of the real one. -->
    <Grid Grid.IsSharedSizeScope="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="260"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Meta-action bar: Menu/Save Game (left, CompactButtonStyle) and the turn-phase
                 tracker (centered) — moved up here so the stats/actions row below isn't crowded. -->
            <Grid Grid.Row="0" Margin="0,0,0,6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" SharedSizeGroup="MetaBarRow"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="MetaBarLeft"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="MetaBarRight"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Style="{StaticResource CompactButtonStyle}" Content="Menu" Margin="0,0,8,0" Command="{Binding BackToMenuCommand}"/>
                    <Button Style="{StaticResource CompactButtonStyle}" Content="Save Game" Margin="0,0,8,0" Command="{Binding OpenSaveGamePanelCommand}"/>
                    <Button Style="{StaticResource CompactButtonStyle}" Content="?" Command="{Binding OpenHelpCommand}"
                            ToolTip="Keyboard shortcuts"/>

                    <TextBlock Text="{Binding ConnectionLostMessage}" Foreground="IndianRed" VerticalAlignment="Center" Margin="12,0,0,0"
                               Visibility="{Binding ConnectionLostMessage, Converter={StaticResource NullToVisibility}}"/>
                </StackPanel>

                <!-- Wake Up => Materialization => Recollection => Draw => Main => End, with the
                     current phase highlighted. A fixed set of 6, so written out directly rather
                     than templated from TurnPhase's values. The right-only margin nudges the
                     centered group left of true-center (a right margin still counts toward what
                     gets centered, so it shifts the visible content left without needing a mirrored
                     spacer column). -->
                <ContentControl Grid.Column="1" Content="{Binding}" ContentTemplate="{StaticResource PhaseBarTemplate}"
                                      HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,100,0"/>
            </Grid>

            <!-- Stats/actions row: the Life/Turn/opponent stats box (left), Roll/Opponent/Damage
                 (right). The phase tracker used to live in the middle column here — moved up to the
                 meta-action bar above so this row has more breathing room. -->
            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Stats area: Life, Turn number, the turn badge, (online) the opponent's Life, and
                     the match clock — grouped in their own bordered box rather than loose in the
                     header, so this doesn't visually blend into Roll/Damage/Opponent on the other
                     side. -->
                <Border Grid.Column="0" Background="#2D3748" CornerRadius="8" Padding="12,6" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource CounterButtonStyle}" Content="-" Command="{Binding DecreaseLifeCommand}"/>
                        <TextBlock Text="{Binding Player.Life, StringFormat='Life: {0}'}" FontSize="16" FontWeight="Bold"
                                   Foreground="{Binding Player.Life, Converter={StaticResource LowLifeToBrush}}"
                                   VerticalAlignment="Center" Margin="8,0"/>
                        <Button Style="{StaticResource CounterButtonStyle}" Content="+" Command="{Binding IncreaseLifeCommand}"/>

                        <TextBlock Text="{Binding Stats.TurnCount, Converter={StaticResource TurnCountDisplay}, StringFormat='   Turn {0}'}" FontSize="16"
                                   Foreground="#F7FAFC" VerticalAlignment="Center" Margin="16,0,0,0"/>

                        <!-- Online only: whose turn it currently is — drives the same IsMyTurn that
                             gates Advance Phase and auto-opens/closes the opponent panel. Green
                             while it's actually yours, the same muted gray as before otherwise. -->
                        <Border CornerRadius="10" Padding="10,3" Margin="16,0,0,0"
                                VerticalAlignment="Center"
                                Visibility="{Binding IsOnline, Converter={StaticResource BoolToVisibility}}">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#F6AD55"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMyTurn}" Value="True">
                                            <Setter Property="Background" Value="#48BB78"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock FontSize="13" FontWeight="Bold" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Opp Turn"/>
                                        <Setter Property="Foreground" Value="#1A202C"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsMyTurn}" Value="True">
                                                <Setter Property="Text" Value="Your Turn"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>

                        <!-- Online only: always-visible opponent Life, boxed (unlike the plain Life/
                             Turn/clock text) so it stands out as its own thing, and gold for 1s
                             right after it changes (GameBoardViewModel.FlashOpponentLifeChanged). -->
                        <Border Background="#1A202C" BorderBrush="#4A5568" BorderThickness="1" CornerRadius="8"
                                Padding="8,3" Margin="16,0,0,0" VerticalAlignment="Center"
                                Visibility="{Binding IsOnline, Converter={StaticResource BoolToVisibility}}">
                            <TextBlock FontSize="16" FontWeight="Bold"
                                       Text="{Binding OpponentLife, StringFormat='Opp Life: {0}'}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{Binding OpponentLife, Converter={StaticResource LowLifeToBrush}}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding OpponentLifeJustChanged}" Value="True">
                                                <Setter Property="Foreground" Value="#F6E05E"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>

                        <!-- Match clock — online games only (live, or viewing a saved online game
                             later, in which case this is frozen at whatever it read when saved and
                             never resumes; see GameBoardViewModel.ShowElapsedTime/ElapsedTime). -->
                        <TextBlock Text="{Binding ElapsedTime, StringFormat={}{0:hh\\:mm\\:ss}}" FontSize="16" FontWeight="Bold"
                                   Foreground="#F7FAFC" VerticalAlignment="Center" Margin="16,0,0,0"
                                   Visibility="{Binding ShowElapsedTime, Converter={StaticResource BoolToVisibility}}"/>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Online only: opens the read-only opponent panel (also auto-opens/closes on
                         each turn handoff — see GameBoardViewModel.UpdateIsMyTurn). Light orange
                         (OpponentButtonStyle), filling solid when they've done something since the
                         panel was last open. -->
                    <Button Style="{StaticResource OpponentButtonStyle}" Content="Opponent" Margin="0,0,16,0"
                            Command="{Binding ToggleOpponentPanelCommand}"
                            Visibility="{Binding IsOnline, Converter={StaticResource BoolToVisibility}}"/>

                    <!-- Always visible, unlike Opponent — Sealed is a private zone of your own, usable
                         solo too, not an online-only concept. Also a drop target in its own right
                         (not just the panel's internal one): the panel is a full-board modal like
                         every other overlay here, so it hides Hand/Field/etc. while open — there'd
                         be no way to drag INTO Sealed from elsewhere without this, since you can't
                         see both at once. -->
                    <Button Style="{StaticResource ActionButtonStyle}" Margin="0,0,16,0" Command="{Binding ToggleSealedPanelCommand}"
                            behaviors:CardDropBehavior.TargetZone="Sealed" behaviors:CardDropBehavior.MoveCommand="{Binding MoveCardToCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Sealed" VerticalAlignment="Center"/>
                            <Border Background="#1A202C" CornerRadius="8" Padding="6,1" Margin="6,0,0,0">
                                <TextBlock Text="{Binding Sealed.Cards.Count}" FontWeight="Bold"/>
                            </Border>
                        </StackPanel>
                    </Button>

                    <!-- Banish/Reveal/Glimpse/Mill/Give — click to open a menu instead of the old
                         invisible keyboard chords, per GameBoardViewModel's own ArmedChord family. -->
                    <Button Style="{StaticResource ActionsButtonStyle}" Content="Actions" Margin="0,0,16,0" Command="{Binding OpenActionsMenuCommand}"/>

                    <Button Style="{StaticResource ActionButtonStyle}" Margin="0,0,16,0" Padding="10,4"
                            Command="{Binding OpenDicePanelCommand}" ToolTip="Roll dice">
                        <!-- A drawn die (rounded outline + five pips) rather than an image, so it stays
                             crisp and matches the button's text color. -->
                        <Grid Width="24" Height="24">
                            <Border BorderBrush="#F7FAFC" BorderThickness="2" CornerRadius="6"/>
                            <Ellipse Width="4" Height="4" Fill="#F7FAFC" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="5,5,0,0"/>
                            <Ellipse Width="4" Height="4" Fill="#F7FAFC" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,5,0"/>
                            <Ellipse Width="4" Height="4" Fill="#F7FAFC" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Ellipse Width="4" Height="4" Fill="#F7FAFC" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="5,0,0,5"/>
                            <Ellipse Width="4" Height="4" Fill="#F7FAFC" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,5,5"/>
                        </Grid>
                    </Button>

                    <!-- Solo only: with a real opponent, their own Life (visible in the Opponent
                         panel) is the meaningful running total — see GameBoardViewModel's own note
                         on why an arbitrary "damage dealt" tally stops meaning much online. -->
                    <StackPanel Orientation="Horizontal" Visibility="{Binding IsSolo, Converter={StaticResource BoolToVisibility}}">
                        <Button Style="{StaticResource CounterButtonStyle}" Content="-" Command="{Binding DecreaseDamageDealtCommand}"/>
                        <TextBlock Text="{Binding Stats.DamageDealtCount, StringFormat='Damage: {0}'}" FontSize="18" FontWeight="Bold"
                                   Foreground="#F7FAFC" VerticalAlignment="Center" Margin="8,0"/>
                        <Button Style="{StaticResource CounterButtonStyle}" Content="+" Command="{Binding IncreaseDamageDealtCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <!-- The board: Field, Memory, and Hand are stacked as distinct, non-overlapping regions
                 (not layered on top of each other) — Material sits to their left, Banishment/Main/
                 Graveyard to their right. -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <!-- Fixed (not Auto) so Hand reserves its space even when empty — Auto would
                         collapse it to zero height with no cards, shifting Memory's position
                         every time the hand size changed. Tall enough for one row of full-size
                         (125x175, +margin) cards; the Viewbox below only shrinks cards when a
                         big hand doesn't fit the row's width, not because of this height. -->
                    <RowDefinition Height="190"/>
                </Grid.RowDefinitions>

                <!-- Champion: top-left, where Material used to sit. Only Champion-type cards may
                     enter (enforced in GameSession.MoveCard) and a new arrival auto-stacks on top;
                     face-up, unlike Material/Main, since it's the champion currently in play.
                     ShowOverlay=False: no count/scrim, just the top champion's own art. -->
                <views:StackZoneView Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Top"
                                     Margin="0,20,0,0"
                                     Zone="{Binding Champion}" HeaderText="Champion" ShowBorder="True" ShowOverlay="False" IsZoomable="True"
                                     TargetZone="Champion" MoveCommand="{Binding MoveCardToCommand}"
                                     PeekCommand="{Binding PeekZoneCommand}"/>

                <!-- Material: moved down to roughly align with Main over in the right-hand stack
                     (approximate — that stack is a StackPanel, not a Grid, so there's no exact
                     row to match against). -->
                <views:StackZoneView Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Top"
                                     Margin="0,240,0,0"
                                     Zone="{Binding MaterialDeck}" HeaderText="Material" IsFaceDown="True"
                                     TargetZone="MaterialDeck" MoveCommand="{Binding MoveCardToCommand}"
                                     PeekCommand="{Binding PeekZoneCommand}"/>

                <!-- Tokens: bottom-left, roughly in line with Graveyard over in the right-hand
                     stack. A permanent, always-populated catalog — click to browse every token in
                     the game and drag one onto the Field to spawn a fresh copy (GameSession.
                     MoveToken); drag a spawned copy back here to discard it, since that's the only
                     other zone a token can ever go to. -->
                <views:StackZoneView Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Top"
                                     Margin="0,450,0,0"
                                     Zone="{Binding Tokens}" HeaderText="Tokens" ShowBorder="True"
                                     TargetZone="Tokens" MoveCommand="{Binding MoveCardToCommand}"
                                     PeekCommand="{Binding PeekZoneCommand}"/>

                <!-- Field: a bounded region (not the whole board) — drop a card anywhere in it and
                     it stays exactly there. No border or label. -->
                <ItemsControl x:Name="FieldItemsControl" Grid.Row="0" Grid.Column="1" Margin="8"
                              ItemsSource="{Binding Field.Cards}" ItemTemplate="{StaticResource CardTemplate}"
                              Background="Transparent"
                              behaviors:CardDropBehavior.TargetZone="Field"
                              behaviors:CardDropBehavior.MoveCommand="{Binding MoveCardToCommand}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <!-- Canvas never sizes itself to its container (it always measures as
                                 0x0, regardless of available space) — children still render/hit-test
                                 fine at their absolute positions, but the "empty" surrounding area
                                 wouldn't be part of the Canvas's own bounds at all without this,
                                 meaning it wouldn't be droppable. -->
                            <Canvas Background="Transparent"
                                    Width="{Binding ActualWidth, ElementName=FieldItemsControl}"
                                    Height="{Binding ActualHeight, ElementName=FieldItemsControl}"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Left" Value="{Binding Instance.FieldX}"/>
                            <Setter Property="Canvas.Top" Value="{Binding Instance.FieldY}"/>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                </ItemsControl>

                <!-- Memory: bordered bar, upright cards, directly below Field. Shrinks to fit like
                     Hand (Viewbox, StretchDirection=DownOnly — never scales up, only down) instead
                     of scrolling, so a full row is visible at a glance rather than needing to
                     scroll through it. The drop target is a Border wrapping the Viewbox, not the
                     GroupBox itself — Viewbox is a bare Decorator with no Background of its own,
                     so once its content shrinks, the empty margin around it isn't hit-test visible
                     and drag-enter stops registering there. Same reasoning as Hand's own Border. -->
                <GroupBox Grid.Row="1" Grid.Column="1" Header="Memory" Width="950" HorizontalAlignment="Center" Margin="0,8,0,8"
                          Foreground="#F7FAFC" BorderBrush="#4A5568">
                    <Border Height="190" Background="Transparent"
                            behaviors:CardDropBehavior.TargetZone="Memory"
                            behaviors:CardDropBehavior.MoveCommand="{Binding MoveCardToCommand}">
                        <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
                            <ItemsControl ItemsSource="{Binding Memory.Cards}" ItemTemplate="{StaticResource CardTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </Viewbox>
                    </Border>
                </GroupBox>

                <!-- Hand: bottom, no border or label. Spans under Material too (col 0-1, not col 2 —
                     that's reserved the full height for Banishment/Main/Graveyard, so extending
                     into it would make Hand a second overlapping drop target there) for extra
                     width before a big hand needs to shrink. The Border fills the whole row
                     (Background=Transparent, same reasoning as Field's Canvas) so it's a stable,
                     permanently-present drop target no matter how small the Viewbox scales the
                     cards down to. The Viewbox only ever shrinks (StretchDirection=DownOnly) —
                     a hand that already fits renders at full size, centered. The left-only margin
                     nudges that centered content right of where column 0-1's own true center sits
                     (missing column 2's width on the right otherwise biases it left). -->
                <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="100,0,0,8" Background="Transparent"
                        behaviors:CardDropBehavior.TargetZone="Hand"
                        behaviors:CardDropBehavior.MoveCommand="{Binding MoveCardToCommand}">
                    <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
                        <ItemsControl ItemsSource="{Binding Hand.Cards}" ItemTemplate="{StaticResource CardTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <!-- Hand-only: cards rest at CardTemplate's own normal 125x175 size (no
                                 shrinking) and pop larger on hover, growing from the bottom edge so
                                 they lift toward the viewer instead of just downward. Scoped to this
                                 ItemsControl's own ItemContainerStyle rather than touching
                                 CardTemplate itself, which Field/Memory/Glimpse/the peek popups all
                                 share. -->
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="RenderTransformOrigin" Value="0.5,1"/>
                                    <Setter Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Panel.ZIndex" Value="10"/>
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.3" Duration="0:0:0.12"/>
                                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.3" Duration="0:0:0.12"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.12"/>
                                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.12"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                        </ItemsControl>
                    </Viewbox>
                </Border>

                <!-- Banishment / Main / Graveyard: right-edge column, spanning the full height. Banishment is a sideways stack. -->
                <StackPanel Grid.Row="0" Grid.RowSpan="3" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <views:StackZoneView Margin="8,50,0,16"
                                         Zone="{Binding Banishment}" HeaderText="Banishment" ShowBorder="True" IsSideways="True"
                                         TargetZone="Banishment" MoveCommand="{Binding MoveCardToCommand}"
                                         PeekCommand="{Binding PeekZoneCommand}"/>
                    <views:StackZoneView Margin="8,20,0,16"
                                         Zone="{Binding MainDeck}" HeaderText="Main" IsFaceDown="True" ClickCommand="{Binding DrawCardCommand}" ShiftClickCommand="{Binding DrawCardIntoMemoryCommand}"
                                         TargetZone="MainDeck" MoveCommand="{Binding MoveCardToCommand}"
                                         PeekCommand="{Binding PeekZoneCommand}"/>
                    <views:StackZoneView Margin="8,20,0,0"
                                         Zone="{Binding Graveyard}" HeaderText="Graveyard" ShowBorder="True"
                                         TargetZone="Graveyard" MoveCommand="{Binding MoveCardToCommand}"
                                         PeekCommand="{Binding PeekZoneCommand}"/>
                </StackPanel>
            </Grid>
        </Grid>

        <gb:StatsPanelView Grid.Column="1"/>

        <gb:PeekOverlayView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:ReviewPanelView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:RevealPanelView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:GlimpseOverlayView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:SaveGameOverlayView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:HelpOverlayView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:OpponentPanelView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:SnapshotPilePopupView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:SealedPanelView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:DiceOverlayView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:ActionsMenuView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:GenerateSearchView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:GenerateCountView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:GiveTargetPickerView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:ZoomOverlayView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:OpponentRevealOverlayView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:SnapshotZoomOverlayView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:SideboardPanelView Grid.Column="0" Grid.ColumnSpan="2"/>

        <gb:NewGameAgreementView Grid.Column="0" Grid.ColumnSpan="2"/>

        <!-- Undo — shown toast-style (bottom-center, same visual language as the opponent notice
             below) rather than a small header button, so it's easy to spot and click in time. Real
             Button, not just a notice: clicking anywhere on it fires Undo. Non-modal (excluded from
             IsAnyOverlayOpen — see its own comment), so it never blocks a keyboard shortcut. -->
        <Button Grid.Column="0" Grid.ColumnSpan="2" Command="{Binding UndoCommand}" Focusable="False" Cursor="Hand"
                HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,24"
                Visibility="{Binding IsUndoAvailable, Converter={StaticResource BoolToVisibility}}"
                d:IsHidden="True">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="#2D3748" BorderBrush="#F6E05E" BorderThickness="2" CornerRadius="8" Padding="16,8">
                        <StackPanel Orientation="Horizontal">
                            <views:CountdownRingView RemainingFraction="{Binding UndoRemainingFraction}" Diameter="28" Thickness="4" Margin="0,0,10,0"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Foreground="White" FontWeight="Bold">
                                    <Run Text="Undo"/>
                                    <Run Text="{Binding UndoActionLabel, StringFormat=' {0}'}"/>
                                </TextBlock>
                                <!-- Optional second line for context a short action label can't
                                     convey on its own — e.g. Generate clarifying the new cards went
                                     to the bottom rather than being shuffled in. -->
                                <TextBlock Text="{Binding UndoDetailMessage}" Foreground="#CBD5E0" FontSize="11"
                                           Visibility="{Binding UndoDetailMessage, Converter={StaticResource NullToVisibility}}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <!-- Opponent's Undo notice toast — non-modal (IsHitTestVisible=False, not part of
             IsAnyOverlayOpen), so it never blocks input or a keyboard shortcut the way every real
             overlay above does. -->
        <Border Grid.Column="0" Grid.ColumnSpan="2" Background="#2D3748" BorderBrush="#F6E05E" BorderThickness="2" CornerRadius="8"
                Padding="16,8" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,24"
                Visibility="{Binding ToastMessage, Converter={StaticResource NullToVisibility}}" IsHitTestVisible="False"
                d:IsHidden="True">
            <TextBlock Text="{Binding ToastMessage}" Foreground="White" FontWeight="Bold"/>
        </Border>
    </Grid>
</UserControl>
